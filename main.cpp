#include "vector/vector.h"
#include <iostream>


using namespace std;

int main() {
  vector *test = new vector();
  test->push_back(1);
  test->push_back(3);
  test->push_back(1);
  test->push_back(3);
  test->push_back(1);
  test->push_back(3);
  
}